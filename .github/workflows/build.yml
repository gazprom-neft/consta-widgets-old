name: Тесты и деплой
on:
  pull_request:
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [12.x]
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: 12
          registry-url: 'https://npm.pkg.github.com'
      - name: Установка yarn
        id: yarn-setup
        run: |
          yarn policies set-version 1.22.1
          echo "::set-output name=cache_dir::$(yarn cache dir)"
      - name: Настройка кэширования зависимостей
        uses: actions/cache@v2
        with:
          path: ${{ steps.yarn-setup.outputs.cache_dir }}
          key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            ${{ runner.os }}-yarn-
      - name: Установка зависимостей
        run: yarn --frozen-lockfile
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_GITHUB_REGISTRY_TOKEN }}
      - name: Сборка пакета build
        run: yarn build
      - name: Сборка пакета build:css
        run: yarn build:css
      - name: Сборка пакета build:js
        run: yarn build:js
      - name: Сборка пакета build:types
        run: yarn build:types
      - name: Сборка пакета build:package
        run: yarn build:package.json
      - name: Сборка пакета build:docs
        run: yarn build:docs
